#include <Windows.h>
#include <WinBase.h>
#include <WinUser.h>

INT WINAPI WinMain(_In_ HINSTANCE hInstance, _In_ HINSTANCE hPrevInstance, _In_ LPSTR lpCmdline, _In_ INT nShowCmd) {
	//MessageBox(NULL, "Hello!", "Program2", MB_OK);
}